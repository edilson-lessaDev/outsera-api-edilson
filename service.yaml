apiVersion: v1
kind: Service
metadata:
  name: outsera-api
spec:
  type: NodePort
  selector:
    app: outsera-api
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081